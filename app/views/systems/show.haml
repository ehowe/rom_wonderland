#games
  %h3 Total Number of games: #{@system.games.count}
  %table
    %thead
      %th Name
      %th Download link
    %tbody
      - @games.each do |game|
        %tr
          %td= game.name
          %td= link_to "Download", game.rom.url(:original, false)


  = link_to "New game", new_systems_game_path(@system.id)
